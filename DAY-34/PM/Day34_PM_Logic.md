# Day 34 PM – Session Logic

## Overall plan
This assignment acts as a complete Week 6 reference, so the files are separated by part and kept simple.
Part A is the main review notebook, Part B is the PCA compression demo, Part C is interview preparation, and Part D is a verified study guide.

## Part A logic
- Cover all required algorithms and techniques with short, repeatable reference sections.
- Keep every section uniform: description, use case, hyperparameters, and tiny code example.
- Use the Wine dataset only to verify 3 representative supervised models so the notebook stays lightweight and reliable.
- Add a text-based flowchart to make algorithm choice easier during revision.

## Part B logic
- Use a generated RGB image so the notebook is fully self-contained.
- Save the image and load it with `matplotlib.pyplot.imread` to match the requirement.
- Apply PCA separately to each color channel and reconstruct images for different component counts.
- Report both compression ratio and reconstruction MSE.

## Part C logic
- Focus on a realistic ML pipeline answer from raw data to deployment.
- Provide a reusable weekly comparison function with optional PCA.
- Explain clearly why preserving variance does not guarantee preserving target signal.

## Part D logic
- Build a structured study guide covering all Week 6 topics.
- Verify common weak points in AI-generated guides and add the missing concepts.
- End with a short revision order so the file is practical, not just descriptive.
